# Tomcat
server:
  port: 9205
  servlet:
    context-path: /v1/user
mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  type-aliases-package: blossom.project.towelove.user.domain
spring:
  profiles:
    active: @activeProperties@
