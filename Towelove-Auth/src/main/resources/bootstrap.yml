spring:
  application:
    # application-name
    name: towelove-auth
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        file-extension: yaml
        shared-configs[0]:
          data-id: towelove-base-dev.yaml
          refresh: true
        shared-configs[1]:
          data-id: towelove-redis-dev.yaml
          refresh: true
        shared-configs[2]:
          data-id: towelove-auth-dev.yaml
          refresh: true

